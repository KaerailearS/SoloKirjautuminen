// shortcut assignment for toggling admin panel
const SHORTCUT_KEYS = {
  ADMIN_PANEL_TOGGLE: {
    ctrlKey: true, // currently only CTRL is needed to be held
    shiftKey: false,
    altKey: false,
    code: "F2", // change to whatever key you want it to be
  },
};

export default SHORTCUT_KEYS;
